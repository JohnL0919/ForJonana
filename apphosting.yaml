# Firebase App Hosting configuration
# This file configures your Next.js app for Firebase App Hosting

# Runtime configuration
runtime:
  nodeVersion: "18"

# Build configuration
build:
  buildCommand: "npm run build"
# Environment variables will be set directly in Firebase Console
# Go to Firebase Console > App Hosting > Environment Variables
# Add each NEXT_PUBLIC_* variable there
